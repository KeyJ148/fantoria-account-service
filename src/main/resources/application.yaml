spring:
  application:
    name: fantoria-account-service

  jpa:
    database: POSTGRESQL
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

  liquibase:
    change-log: db/liquibase-master.yaml